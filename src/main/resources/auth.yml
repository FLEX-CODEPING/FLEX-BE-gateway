jwt:
  secret-key: ${JWT_ACCESS_SECRET_KEY:exampleSecretKeyForNamoSystemAccessSecretKeyTestForPadding}
  expiration-time:
    access-token: ${JWT_ACCESS_EXPIRATION_TIME:1209600}
    refresh-token: ${JWT_REFRESH_EXPIRATION_TIME:1800}

---
spring.config.activate.on-profile: local

---
spring.config.activate.on-profile: dev

---
spring.config.activate.on-profile: prod